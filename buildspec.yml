version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 8
  build:
    commands:
      - npm install
      - npm run webpack --production
artifacts:
  files:
    - index.html
    - favicon.ico
    - Bryce_Frazier_Resume.pdf
    - styles/*
    - images/*
    - dist/bundle.js
